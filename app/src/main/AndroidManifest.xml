<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Local-first: o app funciona 100% offline. Rede é PERMITIDA, mas só para features
      opt-in (ex.: backup no Drive, modelos pagos). Adicione INTERNET apenas quando uma
      dessas features existir. A task `reportNetwork<Variant>` lista as permissões de rede
      no build (informativo) para telemetria acidental não passar despercebida.

      Permissões reais serão adicionadas por fase, conforme necessário:
        RECORD_AUDIO, FOREGROUND_SERVICE, FOREGROUND_SERVICE_MICROPHONE,
        POST_NOTIFICATIONS, ACCESS_FINE_LOCATION, ACCESS_BACKGROUND_LOCATION,
        USE_BIOMETRIC, RECEIVE_BOOT_COMPLETED,
        INTERNET (quando houver sync/modelos remotos).
    -->

    <application
        android:name=".MemoraApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Memora">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Memora">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
